<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dxe="clr-namespace:DevExpress.XamarinForms.DemoEditors;assembly=DevExpress.XamarinForms.DemoEditors"
    x:Class="Stocks.UI.Views.MenuHeader">

    <Grid
        Padding="24, 0, 24, 8"
        RowSpacing="0"
        ColumnSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <dxe:IconView
            ImageSource="round_logo.svg"
            Grid.Row="0"
            Grid.Column="0"
            WidthRequest="40"
            HeightRequest="40"
            Margin="0, 20"/>

        <Label
            Grid.Row="0" Grid.Column="1"
            Margin="20,0,0,0"
            Text="Stocks"
            FontSize="Title"
            FontAttributes="Bold"
            VerticalTextAlignment="Center"
            TextColor="{DynamicResource TextColor}"/>

        <BoxView
            Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
            HeightRequest="1"
            Color="{DynamicResource DrawerSeparatorColor}"/>
    </Grid>


</ContentView>
